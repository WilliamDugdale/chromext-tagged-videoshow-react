import React, { Component } from 'react';

export default class Multibox extends Component {
    render () {
        return (
            <div id="multibox">
                <p>Choose a tag</p>
            </div>
        );
    }
}